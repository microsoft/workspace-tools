# Repo has been merged into [`lage`](https://github.com/microsoft/lage)

The `workspace-tools` repo has been merged into the [`lage` repo](https://github.com/microsoft/lage) for easier management. You can find the `workspace-tools` package under `packages/workspace-tools`.
